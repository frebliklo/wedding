backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)

media_folder: static/assets
public_folder: assets

collections:
  - name: onsker
    label: Ønskeliste
    folder: "src/data/onsker"
    create: true
    fields:
      - { name: text, label: Text }
      - { name: link, label: Link }